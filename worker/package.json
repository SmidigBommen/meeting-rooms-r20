{
  "name": "meeting-room-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "TZ=UTC vitest run",
    "test:watch": "TZ=UTC vitest"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "wrangler": "^4.76.0"
  }
}
